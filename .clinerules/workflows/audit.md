# Cline Audit

Read `.agents/skills/yurunavi-audit/SKILL.md` and follow its read-only procedure. Load only the `.ai/` policy relevant to the diff. Return PASS or FAIL in concise Korean; never edit or commit during audit.
