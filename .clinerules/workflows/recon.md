# RECON (read-only)
You are investigating only. Do NOT modify code or commit. Absolutely no writes.

1. Trace the target symptom with grep. Cite every finding as `file:line`.
2. Read related functions and data flow with read_file to confirm behavior.
3. Save findings to `loop/RECON_{topic}.md`. Record facts only, not hypotheses.
4. End by listing root-cause candidates, then STOP. Do not produce a SPEC or write any implementation.

Report to the user in Korean.