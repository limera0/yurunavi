# CLAUDE.md — YuruNavi Claude Adapter

@loop/STATUS.md

Claude Code follows the shared operating kernel in `AGENTS.md`. Do not duplicate shared policy here.

## Startup

- `loop/STATUS.md` is injected above and generated by `loop/gen_status.sh`; never hand-edit it.
- Use STATUS line references to read only relevant `loop/RELEASE_ROADMAP.md` ranges.
- Search `loop/WIKI_INDEX.md` before a new investigation.
- Load only the `.ai/` documents routed by `AGENTS.md`; never preload the whole directory.

## Claude execution

- Unattended work uses `loop/run_night_auto.sh`: short independent `claude -p` ticks, task file plus `loop/.auto/handoff.md`, one checkpoint per tick.
- Act as control plane. Delegate meaningful `lib/` work to `flutter-coder`, `native/` work to `rust-coder`, and completed changes to `code-auditor`.
- Coders do not commit. The orchestrator verifies audit/tests and uses `.ai/GIT_SAFETY.md` before committing.
- Use `.ai/WORKFLOW.md` for implementation/unattended work, `.ai/REPORTING.md` at handoff, and other shared documents only when their task trigger applies.
- Ask the Product Owner only under the escalation boundary in `AGENTS.md`; all owner-facing communication is concise plain Korean.

## Claude environment

- Repository scope is `/data/projects/yurunavi`; no external-system mutation without explicit authority.
- Default unattended permission mode is `bypassPermissions` as configured by the runner.
- Keep ticks short to preserve context and avoid mid-stream connection failure.
