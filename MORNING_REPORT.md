# 아침 보고서 (작성 중…)

작성 날짜: 2026-06-01

---

## 모듈 0 — 베이스라인 진단 결론

### 경로 탐색이 언제, 왜 깨졌는가?

| 커밋 | 내용 | 영향 |
|------|------|------|
| `fba4054` | OSM 타일 전환 + `&exclude=motorway` 추가 | **경로 탐색 전면 실패** |
| `8d0da1a` | `exclude=motorway` 제거 | 경로 탐색 복구 |

**원인:** OSRM 공용 데모 서버(`router.project-osrm.org`)는 `exclude=` 파라미터를 지원하지 않아 HTTP 400을 반환. 결과적으로 모든 경로 요청이 빈 폴리라인으로 처리됨.

**판단: (b) 현재 코드에서 앞으로 진행**
- 이미 `8d0da1a`에서 수정 완료. 되돌릴 필요 없음.
- OSM 타일은 유지. 경로 서비스는 현재 정상 URL 사용 중.
- `flutter analyze` 이상 없음 (0 issues).

### 남은 회귀 이슈 (→ 모듈 1에서 수정)
1. 3개 코스 카드 각각이 별도 경로를 즉시 그리지 않음 (목적지 확정 시 기본 선택 카드 하나만 경로를 페치함)
2. 반투명 원: 코드상 존재하나 동작 검증 필요
3. 편의점 스냅: 코드상 존재하나 동작 검증 필요

---

*(모듈 1~5 진행 후 아래에 추가 예정)*
