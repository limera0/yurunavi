GOAL: 후면단속카메라 게이지(rear_camera_gauge.dart)를 원작 레퍼런스 이미지(speedcam1/2.webp)와 정밀 대조해 점/스포크 개수·간격을 실제와 일치시킨다

---

# HANDOFF — 후면단속카메라 게이지 다이얼 정밀화 — 2026-07-29

## 배경

이전 세션(`e87686e6`, 08:41~13:00 약 10시간, 여러 차례 resume로 컨텍스트 누적)이
레퍼런스 이미지 픽셀 분석 중 **API Error: Unable to connect to API (ECONNRESET)** 무한 루프에
빠져 마스터가 창을 닫고 세션 종료. 코드 변경 없이 read-only 분석만 하던 중이라 유실된 작업 없음
(`git status` 확인 완료, tracked 파일 변경 0건). 이 문서는 그 분석을 이어받기 위한 핸드오프.

**주의**: 이 정밀화 작업은 이미지 픽셀 분석(Bash + PIL/numpy)을 반복 호출하기 쉬운 성격이라
컨텍스트가 빨리 불어난다. CLAUDE.md 원칙대로 **짧게 끊어서** 진행할 것 — 분석 스크립트는
한 번에 정확하게 짜서 최소 호출로 끝내고, 결론 나면 바로 구현 → 커밋. 절대 이 세션을
몇 시간씩 끌고 가지 말 것.

## 대상 파일

- `lib/features/navigation/presentation/rear_camera_gauge.dart` (584줄)
  - 접근구간(150m→0m) 웨지: `_spokeCount = 40` (L125) — 붉은 스타버스트 스포크 개수
  - 사후구간(0m→postZoneM) 점-링: `_dotCount = 20` (L455) — 원주 점 개수, `_DotRingPainter` (L460~476)
- 마지막 체크포인트 커밋: `0462410` "게이지 디자인 재작업" — 이 커밋 이후 추가 커밋 없음

## 레퍼런스 이미지 (둘 다 untracked, 이번 세션이 08:42 추가)

- `assets/images/speedcam1.webp` — 접근구간(SEC.) 웨지 레퍼런스. "12" 표시, 붉은 스타버스트
  부채꼴(좌측) + 초록 쐐기 + 노란 눈금호(우측) + 노란 테두리. **이미 구현된 웨지 디자인과
  구조적으로 거의 일치** — 정밀 대조 우선순위 낮음.
- `assets/images/speedcam2.webp` — 사후구간(STOP) 점-링 레퍼런스. 전체 빨강 상태(단속중),
  중앙 초록 판독창에 "S"(STOP 앞글자), 원주를 따라 **어두운 적색 점들 + 점 사이 작은 삼각형
  틱마커**가 배치됨. **이번 미해결 분석 대상.**

## 미해결 발견사항 (전 세션 분석 결과, 결론 없음)

`speedcam2.webp`에서 원주 점 개수/각도를 두 가지 방법으로 추출:
- 방법 A ("runs" 기반 연속 픽셀 그룹 카운트): **25개**
- 방법 B (피크 검출 기반 시작각도 리스트): **27개**, 각도(도) —
  `[10.4, 24.3, 39.8, 53.1, 72.1, 72.4, 83.7, 103.9, 107.4, 107.8, 114.1, 131.3, 143.8,
  158.6, 177.2, 196.5, 211.1, 222.9, 238.3, 249.8, 265.3, 277.9, 293.6, 305.5, 320.5, 331.7, 351.8]`
  - 인접 간격을 보면 `72.1°/72.4°`, `107.4°/107.8°` 처럼 0.3~0.4° 차이 쌍이 2건 있음 —
    한 점이 노이즈로 둘로 쪼개져 잡혔을 가능성이 높음. 이 2건을 병합하면 **25개**로 방법 A와 일치.
  - 나머지 점 간격은 대략 11~20° 사이로 흩어져 있어 **완전 등간격은 아닌 것으로 보임**
    (사람 눈에는 등간격처럼 보이나 원본 아이콘 자체가 약간 손으로 그린 배치일 수 있음).
- 현재 코드의 `_dotCount = 20`은 이 25개(추정)와 다름 — 실제로 몇 개로 맞출지, 등간격으로
  근사할지 원본 각도를 그대로 재현할지는 **아직 결정 안 됨**.

## 다음 단계 제안

1. `speedcam2.webp`를 깔끔하게 재분석 — 이전 두 방법이 다른 답을 낸 원인(중복 탐지)을 먼저
   제거: blob 크기/밝기 threshold로 작은 삼각형 틱마커를 점과 분리, connected-component
   라벨링(`scipy.ndimage.label` 등)으로 점 개수를 한 번에 확정. 여러 번 스크립트 재실행하지
   말고 한 번에 정확히 짤 것.
2. 개수 확정되면 등간격 재현이 디자인 의도에 맞는지 마스터에게 확인 (실제 각도 그대로 박아넣는
   것은 과도한 정밀화일 수 있음 — 등간격 근사가 실용적일 가능성 높음).
3. `_dotCount` 값과 `_DotRingPainter`의 점 크기(현재 반경 4.2/6.5)를 레퍼런스 비율에 맞게 조정.
4. flutter-coder로 구현 → `flutter analyze` + 기존 위젯 테스트 PASS → code-auditor → 체크포인트 커밋.
5. 접근구간 웨지(`speedcam1.webp`)는 이미 구조적으로 일치하므로, 시간 남으면만 미세 대조.

## 건드리지 말아야 할 것

- `git push` 금지
- `git add -A` 금지 (named files만)
- 이 분석 작업 자체를 몇 시간짜리 단일 세션으로 끌지 말 것 (ECONNRESET 재발 원인)

---

## 완료 (2026-07-29 후속 세션)

- [x] 사후구간 점-링: `_dotCount` 20→24, 점 반경 4.2/6.5→6.5/9.5 (커밋 `160591a`)
- [x] 접근구간 웨지: `speedcam1.webp` 극좌표 샘플링(반경 0.5~0.9×R 교차검증, 채도
      임계값으로 SEC 박스 그림자/주황 눈금 오염 제거)으로 실제 스타버스트 구간을
      78°~178°(100°)로 특정, 그 안 seam 7개 검출(피치 14.3°) → 360° 환산 25개.
      `_spokeCount` 40→25로 확정 (커밋 `38484a2`)
- 참고: 레퍼런스는 스포크 사이가 완전 검정 갭이 아니라 옅은 crease 선(밝기 10~15%
  차이)뿐이라 거의 이어붙은 부채꼴에 가까움. 현재 구현은 62% 채움/38% 검정 갭을
  유지(176px 소형 위젯에서 가독성 위한 의도적 스타일화, 원본 그대로 재현 시
  오히려 안 보일 위험) — 필요시 duty-cycle 조정은 별도 논의.
- flutter analyze PASS, test/rear_camera_gauge_test.dart 7건 PASS (두 커밋 모두).
- 이 HANDOFF의 GOAL(점/스포크 개수·간격 실제 일치)은 완료로 판단.
