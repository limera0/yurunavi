# 2026-07-14 실주행 결과 + 추가 버그픽스 계획

전날 밤 배포한 빌드(커밋 `61de570`)로 실제 퇴근길 라이딩 테스트 완료. 체크리스트
17항목 결과 + 라이딩 중 신규 발견 4건(17~20) 정리. 로그 파일
(`ynav_2026-07-14T05-17-26.log`, 05:23~06:04, adb pull로 확보) 대조 분석 포함.

## 체크리스트 결과 요약

**정상 확인(OK) — 액션 불필요**: 2(로그 저장), 3-1(칩 고정 안 됨), 3-3(구버전 칩셋
사라짐), 5(블루투스 덕킹), 6(경로선 라벨), 7-1/7-2(지나온 경로), 9(POI 탭
확인카드), 10(검색 리스트 탭 확인카드), 14(검색창 키보드), 15-1/15-2(약관/라이선스).
9개 항목이 실사용에서 검증 완료됨.

**수정 필요**: 3-2(카페 데이터 오염, 데이터소스 한계로 추정), 4(도착배너 회귀),
8/13/16(내비 화면 POI 구버전), 11(핀 소실), 12(슬라이드 버튼 더 나빠짐), 17~20(신규).

## ▶ 우선순위별 계획

### P0-A. 슬라이드 버튼 — 근본 원인 확진, 재수정 필요 (커밋 `34fc4fd`가 원인)

**사용자 보고**: "반응성이 매우 안좋아졌음. 4~5번을 해도 계속 튕김. 보석 다루듯이
정확히 눌러서 등속도로 정확히 밀어야 함." — 이전 세션에서 "너무 멀리 밀어야
완료됨" 버그를 고친다고 `width: constraints.maxWidth` + `buttonWidth: 52`를
추가했는데, 오히려 체감이 나빠짐.

**원인 재분석** (`slider_button-3.1.0/lib/src/slider.dart` 패키지 소스 재확인):
- 이 패키지는 내부적으로 Flutter의 `Dismissible` 위젯을 그대로 사용함
  (`slider.dart:157`). `Dismissible`은 원래 "스와이프해서 리스트 아이템 삭제"용
  위젯이라 **드래그 거리 비율뿐 아니라 놓는 순간의 velocity(플링 속도)도 함께
  판정**에 들어간다 — 정확한 위치까지 도달해도 손을 천천히 떼면 스프링백,
  거리는 못 채워도 빠르게 플링하면 통과되는 등 "아이폰 밀어서 잠금해제"류
  UX(순수 위치 기반, 관대한 판정)와 판정 로직 자체가 다른 위젯.
- `buttonWidth: 52` 추가로 실제 드래그 가능 영역(`width - buttonWidth`)이 아주
  약간 줄어든 건 맞지만(70→52, 화면폭 대비 미미한 차이), 이걸로 "훨씬 나빠짐"을
  설명하기엔 부족함 — **진짜 원인은 처음부터 이 패키지가 "슬라이드 삭제"용
  위젯이라 "슬라이드 확인" UX에 안 맞았던 것**으로 판단. 이전 세션 수정은
  코드 정적분석/산수로만 검증됐고(`BUGFIX_progress.md` 완료:9 참조) 실기기
  드래그 필감 검증이 원천적으로 불가능했던 것도 한 원인.
- **파라미터를 또 조정하는 미봉책 대신, 이 서드파티 패키지(`slider_button`)를
  걷어내고 순수 `GestureDetector`+`AnimatedPositioned` 기반 커스텀 슬라이드
  위젯으로 교체 권장.** velocity 무관, 순수 위치 비율(예: 85% 이상 밀면 확정,
  놓으면 애니메이션으로 부드럽게 완료/복귀)로 판정하면 아이폰 스타일 그대로
  구현 가능하고, 앞으로 튜닝도 우리 코드 안에서 직접 통제 가능해짐.
- 대상 파일: `lib/core/widgets/slider_start_button.dart` (전면 재작성),
  `pubspec.yaml`에서 `slider_button` 의존성 제거 가능(다른 곳에서 미사용 확인 필요).

### P0-B. 도착배너 회귀 + 경유지 재포함 버그 — 같은 근본 원인으로 추정 (4번+17번 묶음)

**사용자 보고(4번)**: "화면 아래에 종료 안내 카드 안 뜸. 바로 종료되어 버림."
**사용자 보고(17번, 신규)**: "경유지 포함 경로에서, 경유지를 통과한 이후 재탐색을
하면 이미 통과한 경유지가 다시 포함됨 — 유턴해서 다시 그 경유지로 가라고
안내함." 경유지가 여러 개일 때 "경유지 도착"(정차) vs "경유지 통과"(주행 중
지나침)를 구분해서 안내하고, 이후 재탐색에서 통과/도착한 경유지는 제외되어야
한다는 요청. Organic Maps 등 참고 요청.

**코드 추적 결과** (`route_progress_provider.dart:141`,
`nav_screen.dart:305-370`):
- `arrived` 플래그는 순수 `distToDest <= _kArrivalM`(현재 활성 폴리라인 잔여거리)
  로만 판정됨 — **이 "폴리라인"이 경유지 구간 하나짜리 서브 라우트인지 최종
  목적지까지의 전체 라우트인지 구분이 없음.** 오늘 로그에서 실제로 경유지 구간이
  끝날 때마다 `YNAV_ARR`(도착) 로그와 거의 동시에 `YNAV_ROUTE`(새 라우트 로드)
  로그가 반복적으로 찍힘 — 경유지 통과 시 새 서브 라우트로 갈아타는 구조로 보임.
- `nav_screen.dart`의 `_arrived`/`_arrivalBannerVisible`/10초 자동종료 타이머
  전부 이 **구분 없는 `arrived` 플래그 하나에** 걸려있음. 즉 **경유지 구간
  종료 시에도 최종 목적지 도착과 똑같이 도착배너 로직이 발동할 가능성이 높음.**
  경유지에서 속도가 느려진 상태(신호대기, 정차 등)면 종료 게이트가 열려
  10초 뒤 `Navigator.pop()`으로 **내비 화면 전체가 꺼져버릴 수 있음** — 사용자가
  본 "카드도 안 뜨고 바로 종료"와 정확히 부합하는 시나리오(빠르게 지나가는
  경유지 전환 중 배너가 한 프레임 스치듯 렌더링되다 사라졌을 가능성 높음, 라이딩
  중엔 놓치기 쉬움).
- **결론: 4번과 17번은 같은 근본 원인(경유지 구간 종료와 최종 목적지 도착을
  구분 못 하는 구조)일 가능성이 매우 높음.** 두 개를 따로 고치지 말고 하나의
  작업으로 묶어서 처리 권장.

**필요한 설계**: 경유지 상태 머신 도입 —
1. 각 경유지에 `pending`/`arrived`(정차해서 도착 확인)/`passed`(정차 없이
   지나침) 상태 부여.
2. 경유지 반경 진입 시 정차(속도 임계값 이하로 몇 초 유지) → "경유지에
   도착했습니다" 음성 + `arrived` 상태로 마킹. 정차 없이 반경을 빠져나가면 →
   "이번 경유지를 통과했습니다" 음성 + `passed` 상태로 마킹.
3. `arrived`/`passed` 상태인 경유지는 이후 모든 재탐색(수동/자동/이탈)에서
   목적지 리스트에서 제외 — 남은 경유지 + 최종 목적지만 남겨서 재요청.
4. 도착배너/종료버튼/10초 자동종료는 **최종 목적지 세그먼트에서만** 발동하도록
   `arrived` 판정 자체에 "이게 마지막 세그먼트인가"를 추가.
5. 사용자 요청대로 **Organic Maps가 이 부분을 어떻게 처리하는지 먼저 조사**
   (오픈소스라 참고 가능) 후 설계 확정 — 다음 세션 착수 전 리서치 단계 필요.

### P1-A. 내비 화면 POI — main_map_screen.dart 업그레이드가 반영 안 됨 (8/13/16번)

**사용자 보고**: 홈 화면은 아이콘+이름 잘 나오는데, 내비 화면은 "그냥 색깔
아이콘, 갱신 느리거나 안됨, 드래그해도 드래그한 지점 중심으로 갱신 안 됨,
표시 개수 적음."

**코드 확인 결과**: 지난 세션(BUGFIX_progress.md "완료: 8." 2단계, 커밋
`1c66ce9`)의 아이콘+라벨 업그레이드는 **`main_map_screen.dart`에만 적용**되고
`nav_screen.dart`는 그대로임을 직접 확인:
- `nav_screen.dart:798` — 여전히 `addCircleLayer`(원형 점, 아이콘/이름 없음).
  `main_map_screen.dart`는 `addSymbolLayer` + `renderPoiIconPng` 사용(아이콘+
  텍스트).
- `nav_screen.dart:844` `_maybeFetchAmbientPois()` — 조회 기준점이 순수
  `navStateProvider`의 GPS 위치 하나뿐. `main_map_screen.dart`는
  `getVisibleRegion()`으로 실제 지도 뷰포트(팬 이동 반영) 기준으로 조회함 —
  이게 "드래그해도 갱신 안 됨"의 정확한 원인.
- 개수 제한도 `take(10)` 단순 컷(주석에 명시)으로, 홈 화면에 적용된
  `PoiService.selectForAmbientDisplay()`(우선순위+분포 알고리즘)가 여기엔
  아예 적용 안 됨.

**계획**: `nav_screen.dart`의 ambient POI 파이프라인을
`main_map_screen.dart`와 동일하게 맞춤(SymbolLayer 전환, selectForAmbientDisplay
적용). **단, 뷰포트 기준 갱신은 설계 확인 필요** — 내비 중엔 카메라가 항상
라이더를 따라가는 게 기본 동작이라 GPS 기준이 의도적일 수 있음. 사용자가
수동으로 지도를 팬했을 때만 일시적으로 팬 위치 기준으로 전환하는 것으로
보이는데, 이 부분은 다음 세션에 사용자 의도 재확인 후 진행.

### P1-B. 목적지/경유지 핀 소실 (11번)

지난 세션 5단계 감사(커밋 `dd25dff`)에서 마커 중복 생성 버그, 라벨 안 지워지는
버그 2건을 이미 고쳤는데, 이번엔 **핀 자체가 사라지는** 더 심한 증상 보고.
기존에 발견 못 한 별개 회귀일 가능성 — 재조사 필요.

### P1-C. 45도 이상 급커브 안내 누락 (18번, 신규)

좌표 `37.05517, 127.05061` — "교차로가 아닌 도로에서 도로 자체가 90도 좌회전
하는 구간"인데 급커브 음성이 안 나옴. `sharp_turn_left/right` 이벤트는 이미
구현돼있지만(guidance_profile.json `enabled:true`), **Valhalla는 결정 지점
(교차로)에서만 maneuver를 생성** — 순수 도로 형상상의 급커브(교차로 아님)는
애초에 maneuver 자체가 안 나올 가능성이 높음(Y자 삼거리 조사 때 확인한 것과
같은 Valhalla의 특성). 확인되면 **폴리라인 지오메트리 자체에서 곡률을
클라이언트가 직접 계산해 감지하는 방식**이 필요 — 기존 "keep 이벤트 방향 분리"
같은 작은 수정이 아니라 새 기능에 가까운 스코프. 좌표 기준 로컬 Valhalla
`/route`로 실제 maneuver 시퀀스부터 까봐야 함(오늘 세션에서 쓴 방법 재사용 가능).

### P2. 카페/주유소 카테고리 데이터 오염 (3-2번)

**사용자 보고**: "할매국물닭발"이 여전히 카페로 나옴, 회사 근처 주유소 자리에
컨설팅 업체가 나옴. 사용자 스스로도 "데이터소스 문제인 듯"이라 짐작.

**재확인**: 코드(`poi_service.dart:23-26`)는 이전 세션에 라이브 검증된 그대로
`indsSclsCd=I21201`(카페 소분류)만 정확히 사용 중 — 코드 버그 아님. 이전
검증은 **다른 좌표 1곳**에서만 130건 전수 확인한 것이라, 전국 데이터 전체의
정확성을 보장하진 않음 — **소상공인시장진흥공단 원본 데이터 자체가 특정
업체(휴업/업종변경/재분류 지연 등)를 오분류한 케이스**로 추정, 우리 쪽에서
100% 걸러내긴 어려움.
**옵션**: (a) 있는 그대로 인정(정부 공공데이터 특성상 불가피), (b) 업소명에
"닭발/곱창/국밥" 같은 강한 요식업 키워드가 있으면 카페 결과에서 제외하는
클라이언트 휴리스틱 필터 추가(정확도 낮고 유지보수 부담 — 두더지잡기식이 될
가능성 큼). **사용자 의견 필요**(b)를 원하시는지, 아니면 그냥 감안하고 넘어갈지.

### P2-B. 고가/터널 진입 안내 (19번, 신규)

좌표 `37.04956, 127.04374` — 고가도로/지하차도 진입인지 옆길인지 500m/300m/50m
룰로 안내 필요. OSM `layer`/`bridge`/`tunnel` 태그를 현재 앱이 전혀 안 쓰고
있어서, 이 태그 데이터를 라우팅 응답에 포함시키는 것부터 필요한 신규 기능.
스코프 큼 — 별도 세션 권장.

### P2-C. 재탐색 화면에서 3개 코스 전부 안 보임 (20번, 신규)

홈 화면 최초 경로 탐색 시엔 3개 코스(시골길/지방도/국도)가 전부 보이는데
(선택=컬러, 나머지=회색), 재탐색 후엔 새 경로 1개만 표시됨. `_reroute()` 로직이
1개 코스만 재요청하는 것으로 추정 — 확인 필요.

## 판단 보류 (재확인 필요, 코드 수정 안 함)

### 1번. Y자 삼거리 — 로그+코드 교차검증 결과 "이상 없음"으로 나옴

사용자 보고: 카드="우측 유지", 음성="왼쪽 차선을 유지하세요"(불일치).

**로그 대조 결과**: 오늘 로그 전체에서 keep류 TTS 이벤트는 **딱 1건**
(`05:56:09 keep_right_approach` → `05:56:14 keep_right_imminent`,
`YNAV_STEP maneuver=23`)뿐이었음. 서정로/서정북로 위치·"우회전 경로로
잡힘"이라는 사용자 설명과도 일치해 이 사건이 맞는 것으로 보임. 그런데:
- `assets/voice_packs/default_ko.json`에 실제 커밋된 `keep_right_imminent`
  값은 `"오른쪽 차선을 유지하세요"`(카드와 일치하는 정상값) — 재확인 완료.
- `voice_pack_service.dart`엔 하드코딩된 폴백 템플릿이 따로 없음 — 항상 이
  JSON 에셋을 직접 읽음. 캐싱 경로도 없음.
- 즉 **코드·에셋 양쪽 다 "우측"으로 정확히 일치**하는데, 실제로 들린 음성이
  "왼쪽"이었다면 로그만으론 설명이 안 됨.

**판단**: 코드 레벨에서 재현 가능한 근거를 못 찾음. 장시간 라이딩 후 순간적인
인지/기억 착오였을 가능성과, 아직 못 찾은 실제 버그일 가능성 둘 다 열려있음
— CLAUDE.md 원칙대로 **추측성 재수정은 하지 않고 보류**. 다음에 비슷한
Y자/T자 분기를 지날 때 카드 문구와 음성 문구를 한 번 더 정확히 확인해주시면
그때 진짜 버그인지 확정 가능.

## 다음 세션 착수 순서 제안
1. P0-A(슬라이드 버튼 재작성) — 스코프 명확, 독립적, 빠르게 끝남.
2. P0-B(경유지 상태 머신, 4+17번 묶음) — Organic Maps 리서치부터 시작, 스코프
   가장 큼, 안전 관련(도착 판정 오류) 등급 높음.
3. P1-A(내비 화면 POI 업그레이드) — 사용자에게 "드래그 시 갱신" 의도 재확인 후.
4. P1-B(핀 소실) — 재조사.
5. P1-C/P2-B(급커브·고가차도 안내) — 좌표 기준 Valhalla maneuver 덤프부터.
6. P2(카페 데이터 오염) — 휴리스틱 필터 도입 여부 사용자 결정 대기.
7. P2-C(재탐색 3코스 미표시) — 빠르게 확인 가능할 듯.

## 하지 말 것
- 1번(Y자 삼거리)은 재현 근거 없이 코드 재수정하지 말 것 — 다음 실주행 재확인
  결과 기다릴 것.
- 유턴/스위치백 costing 튜닝은 여전히 사용자가 명시적으로 보류한 항목 —
  먼저 꺼내지 말 것.
