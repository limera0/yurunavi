# 유루나비 — Claude Code 야간 자율 개발 플레이북 v1

> 목표: 당신이 출근한 12시간 동안 Claude Code가 **안전하게** 코딩을 진행하고,
> 오후 1시에 일어난 당신은 **5분 안에 결과를 검토**한다.
> 
> 구조: **오케스트레이터(총괄)** → **코더(Flutter / Rust)** → **감사관(audit)**
> 
> 이 파일을 Claude Code에 통째로 주고 "이 플레이북대로 세팅해줘"라고 하면,
> 아래 에이전트 파일과 가드레일을 알아서 만들어 줍니다.

---

## 0. 시작 전 반드시 알아야 할 3가지 (안전·비용)

1. **`--dangerously-skip-permissions`는 권장하지 않습니다.**
   공식 문서: 이 모드는 프롬프트 인젝션·오작동에 **아무 보호도 없습니다.**
   대신 **`--permission-mode auto`** 를 씁니다. (무인 백그라운드 점검용으로 Anthropic이 권장)

2. **root/sudo로 실행 금지.**
   리눅스에서 root나 sudo면 bypass 계열 모드가 **실행 거부**됩니다.
   반드시 **일반 사용자 계정**에서 실행하세요. (확인: 터미널에 `whoami` → root가 아니어야 함)

3. **컨테이너 + git 체크포인트가 진짜 안전망입니다.**
   "권한 플래그"가 아니라 **격리된 환경 + 되돌리기 가능한 git**이 당신을 지켜줍니다.
   
   - 모든 작업 전 git 커밋(체크포인트)
   - 자는 동안 코드는 Docker 컨테이너 안에서만 돌게
   - 위험 명령(`rm -rf`, `git push --force` 등)은 hook으로 차단

---

## 1. 비용 통제 (★ 10만원 예산 생존 전략 ★)

12시간 멀티에이전트는 비싸질 수 있습니다. 아래를 지키세요.

- **정액 구독제 사용** (종량제 API로 12시간 무인 실행 = 위험). 현재 본인 플랜의
  사용 한도를 먼저 확인하세요. (Claude Code 사용량/플랜은 변동되니 실행 전 확인)
- **모델 티어링** — 비싼 모델은 총괄에만:
  - 오케스트레이터 = 똑똑한 모델 (계획·판단)
  - 코더 = 중간 모델 (대량 작업)
  - 감사관 = 중간/저렴 모델 (읽기 위주)
- **하룻밤 = 모듈 1개**로 작업 범위를 좁힐 것. "전부 다 해줘"는 토큰 폭탄.
- 아침마다 사용량을 확인하고, 폭증하면 다음 날 범위를 더 줄인다.

---

## 2. 3-에이전트 구조

```
당신이 자기 전에 "오늘 밤 작업" 1개를 지정
        │
        ▼
┌──────────────────────────────┐
│  오케스트레이터 (메인 세션 = 총괄)    │   ← 계획을 세우고 일을 나눠줌
│   1. 작업을 작은 단계로 쪼갬          │
│   2. 코더에게 시킴                  │
│   3. 끝나면 감사관에게 검사시킴        │
│   4. 감사 통과 → git 커밋            │
│   5. 실패 → 코더에게 수정 지시 (반복)  │
│   6. 아침 보고서 작성                │
└───────┬───────────────┬──────────┘
        ▼               ▼
  [flutter-coder]   [rust-coder]      ← 실제 코드 작성
        └───────┬───────┘
                ▼
          [code-auditor]              ← 코드 검사 (빌드/린트/리뷰)
```

> 중요: 서브에이전트는 **자체 계획(plan) 기능이 없고 즉시 실행**합니다.
> 그래서 "계획"은 반드시 오케스트레이터(메인 세션)가 맡습니다.
> 또한 서브에이전트의 `permissionMode`는 무시되고, 내장 **안전 분류기**가
> 모든 행동을 검사합니다(이게 무인 실행의 추가 안전망).

---

## 3. 에이전트 정의 파일 (Claude Code가 만들 파일들)

아래 파일들을 프로젝트의 `.claude/agents/` 폴더에 만듭니다.
(Claude Code에게 "이 내용으로 파일 만들어줘"라고 하면 됩니다.)

### 3-1. `.claude/agents/flutter-coder.md`

```markdown
---
name: flutter-coder
description: MUST BE USED for all Flutter/Dart UI, widgets, screens, state, and package work in this app. Use for anything under lib/.
tools: Read, Write, Edit, Glob, Grep, Bash
model: sonnet
---
You are a senior Flutter engineer building the Yurunavi motorcycle navigation app.
Rules:
- Follow the module structure in CLAUDE.md exactly. Keep modules independent.
- Never hardcode API keys or secrets. Use .env.
- After writing code, always run `flutter analyze` and report results.
- Make the smallest change that satisfies the task. Do not refactor unrelated code.
- Commit nothing yourself; the orchestrator handles git.
- If a task is ambiguous or risky, STOP and report back instead of guessing.
```

### 3-2. `.claude/agents/rust-coder.md`

```markdown
---
name: rust-coder
description: MUST BE USED for all Rust work under rust/ — the "fun-road" scoring engine, Valhalla costing JSON generation, and flutter_rust_bridge glue.
tools: Read, Write, Edit, Glob, Grep, Bash
model: sonnet
---
You are a senior Rust engineer. You build the route-scoring core for Yurunavi.
Rules:
- Prioritize stability and correctness over cleverness.
- After changes, run `cargo build` and `cargo test` and report results.
- Do NOT reimplement a routing graph from scratch. Routing is done by Valhalla;
  your job is costing-rule generation and re-ranking candidate routes.
- Never hardcode secrets. Use .env / config files.
- Commit nothing yourself; the orchestrator handles git.
- If blocked or uncertain, STOP and report back.
```

### 3-3. `.claude/agents/code-auditor.md`

```markdown
---
name: code-auditor
description: MUST BE USED after any coder finishes. Reviews changes for correctness, security, secrets leakage, and whether the build passes. Read-only review — does not edit code.
tools: Read, Glob, Grep, Bash
model: sonnet
---
You are a strict code auditor. You do NOT write or edit code.
Check, in order:
1. Does it build? (flutter analyze / cargo build as relevant)
2. Any hardcoded secrets, API keys, tokens? (search for them) — FAIL if found.
3. Any dangerous shell commands or file deletions introduced? — FLAG them.
4. Does the change match the assigned task and CLAUDE.md module rules?
5. Obvious bugs or broken imports.
Output a short verdict: PASS or FAIL, with a bullet list of issues.
If FAIL, give the coder a precise, minimal fix instruction.
```

---

## 4. 가드레일: `CLAUDE.md` (프로젝트 루트)

이 파일은 모든 세션에 항상 적용되는 "집안 규칙"입니다.

```markdown
# Yurunavi — Project Rules (always apply)

## What we are building
Motorcycle-tourer OSM navigation app. Flutter (UI) + Rust (fun-road scoring) + Valhalla (routing).

## Module structure (keep independent)
lib/core, lib/modules/{map,route_planning,navigation,daylight_bar,settings,auth,tour_summary},
lib/services, rust/, docker/

## Hard rules (never violate)
- NEVER commit secrets. All keys go in .env (which is gitignored).
- NEVER run destructive commands: rm -rf, git push --force, dropping data, mass file deletion.
- NEVER push to a remote unless explicitly told in the night's task.
- Make a git commit BEFORE starting each subtask (checkpoint), and after each PASS.
- One module per night. Do not expand scope beyond the night's assigned task.
- If unsure, STOP and write it in the morning report instead of guessing.

## Workflow each night
1. Orchestrator reads the night's task (from NIGHT_TASK.md).
2. Break into small steps. Checkpoint commit.
3. Delegate to flutter-coder or rust-coder.
4. Run code-auditor. If FAIL, fix and re-audit (max 3 loops, then stop & report).
5. On PASS, commit. Move to next step.
6. At end, write MORNING_REPORT.md: what was done, what passed, what's blocked, token usage note.
```

---

## 5. 위험 명령 차단 (hook)

bypass 모드를 안 쓰더라도, hook으로 위험 명령을 한 번 더 막습니다.
`.claude/settings.json` 의 hooks에 PreToolUse 훅을 걸어, `rm -rf`, `--force`,
`mkfs`, `:(){ ` 같은 패턴이 보이면 차단하도록 설정합니다.
(정확한 hook 문법은 버전마다 다르니, Claude Code에게
"위험한 bash 명령을 차단하는 PreToolUse hook을 현재 버전 문법으로 만들어줘"라고 요청하세요.)

---

## 6. 자기 전 루틴 (매일 밤 5분)

1. 오늘 밤 작업을 `NIGHT_TASK.md` 에 1개만 적는다. 예:
   
   ```
   오늘 밤 작업: lib/modules/map 모듈 완성.
   - OSM 지도가 전체 화면에 뜨고
   - 내 GPS 위치가 중앙에 점으로 표시되고
   - +/- 줌 버튼과 "내 위치로" 버튼 동작
   범위 밖(경로탐색, 내비주행)은 절대 건드리지 말 것.
   ```

2. git 상태가 깨끗한지 확인 (`git status`).

3. 아래 명령으로 무인 실행 시작 (일반 계정, 프로젝트 폴더 안에서):

   ```
   loop/run_night_auto.sh loop/<오늘_밤_작업_파일>.md
   ```

   기본값: 최대 14틱, 전체 10.5시간, 틱당 45분 타임아웃, `bypassPermissions`, effort=high.
   하나의 긴 대화형 세션 대신, 짧고 독립된 `claude -p` 세션("틱")을 스크립트가
   자동으로 이어 붙인다(각 틱은 이전 틱의 대화 기록 없음, `loop/.auto/handoff.md`로만
   상태 인계) — 큰 컨텍스트 + 긴 응답 조합이 서버쪽 `ECONNRESET`을 유발하는 알려진
   문제(anthropics/claude-code#51164)를 피하기 위함.

> 처음 써보는 밤에는 짧은 dry-run으로 먼저 확인할 것 (예:
> `loop/run_night_auto.sh loop/<트리비얼_작업>.md --max-ticks 2 --max-wall-seconds 900 --tick-timeout 300`),
> 지켜보면서 커밋/훅/STATUS 파싱이 정상 동작하는지 확인한 뒤 완전 무인으로 전환하세요.

---

## 7. 일어나서 검토 (오후 1시, 5분)

1. `loop/MORNING_REPORT_<날짜>_*.md` 읽기 — 무엇이 됐고, 뭐가 막혔는지.
2. `git log --oneline` 으로 밤사이 커밋 확인.
3. 앱 한번 실행해서 눈으로 확인 (`flutter run`).
4. 마음에 안 들면 `git reset --hard <체크포인트>` 로 되돌리기 (그래서 체크포인트가 중요).
5. 사용량(토큰) 확인. 폭증했으면 내일 범위 축소.
6. 로그가 더 필요하면: `loop/.auto/logs/run_MMDD_HHMM.log`(하룻밤 전체 요약),
   `loop/.auto/logs/tick_N_*.log`(틱별 상세).

---

## 8. 현실적 기대치 (솔직하게)

- 하룻밤에 "모듈 1개 진행"이 목표지, "앱 완성"이 아닙니다.
- 가끔 밤새 헛돌거나 막혀서 아침에 진척이 0일 수도 있습니다. **정상입니다.**
- 그래서 git 체크포인트와 좁은 범위가 생명입니다. 손실을 하룻밤으로 한정.
- 코드를 못 읽으셔도, **MORNING_REPORT + 앱 실행 화면**으로 검토는 충분히 가능합니다.
- 2~3주에 걸쳐 매일 밤 모듈을 하나씩 쌓으면 MVP에 도달합니다.

---

## 9. 오늘 당장 할 일 (딱 이것만)

1. 서버에서 `whoami` 실행 → root가 아닌 일반 계정인지 확인.
2. 이 플레이북을 Claude Code에 주고:
   "이 플레이북대로 .claude/agents/ 의 에이전트 3개와 CLAUDE.md, 위험명령 차단 hook을
    만들어줘. 아직 앱 코드는 짜지 마. 세팅만."
3. 세팅이 끝나면, 앞서 만든 "첫 작업 지시서(지도 모듈)"를 `NIGHT_TASK.md`로 삼아
   첫 야간 실행을 테스트.

설정이 끝나면 그 결과(폴더 구조나 MORNING_REPORT)를 저에게 보여주세요.
다음으로 Valhalla(Docker + 한국 OSM 데이터) 야간 작업 지시서를 만들어 드리겠습니다.
